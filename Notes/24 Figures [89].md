 

This is the caption. ExampleImage

To modify how styled text is converted to LaTeX code, visit the Markup and Styles compile format panes, when editing this project's "LaTeX (Memoir Book)" format. The Markup pane itself sets how graphics print themselves, which is the \textbackslash{}includegraphics line alone. The stylesheet settings for Figure, Caption and Label determine how the additional syntax is added around that.